import React from "react";
import "../css/Product.css"

export default function DrawProduct(props) {
    const product = props.productToPaint;
    return (
        <div id={"product-container"}>

        </div>
    );
}
